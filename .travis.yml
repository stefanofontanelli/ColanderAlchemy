# Wire up travis
language: python

python:
  - 2.7
  - pypy
  - 3.4
  - 3.5
  - pypy3
  - 3.6
  - 3.7
  - 3.8

#install: python setup.py develop  # install doesn't seem to be needed for testing

script:
  - python setup.py test -q
